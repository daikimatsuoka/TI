# 基本的な出力
System.out.println();
セミコロンをつけないとエラーが起こる
文字列は""で囲む
jabaの処理は処理部分に書いておりその上にメソッド部分、クラス部分が存在

//コメントアウト

数値はダブルクォーテーションで囲まない
クォーテーション内で足し算をするとそのままの文字が出力される
右文字列で足し算をすると文字通り文字列が足し合わされる

足し引き剰余に関してはルビーと同じ


# 変数
文字列のデータの型　String
整数のデータの型　int

変数はデータを入れる箱

変数の型定義　int number;
値代入 number = 3;
 値の表示 System.out.println(number);

値の代入と変数定義は同時に行うことが可能→変数の初期化
int number = 5;

変数定義したものと文字列、数字は連結可能
